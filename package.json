{
  "name": "telefonos",
  "version": "1.0.0",
  "description": "Directorio Corporativo — PHP + Tailwind CSS + Alpine.js + LDAP",
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i css/input.css -o css/style.css",
    "test": "php vendor/phpunit/phpunit/phpunit"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "tailwindcss": "^4.3.0"
  }
}
